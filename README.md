## Watch over dependencies and rebuild
```bash
$ cargo install cargo-watch
$ cargo watch -x run
```

   cargo install diesel_cli
   diesel setup
   diesel migration generate create_post
   diesel migration run
   diesel migration redo
